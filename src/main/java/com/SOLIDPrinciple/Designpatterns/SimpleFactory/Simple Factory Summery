
when every there is object is created on basics of IF-Else comparison
then we use simple design pattern

we put instantiation logic into separate class & mostly to static method

Sometime not considered as DP, as its simply method that encapsulated object instantiation nothing complex


UML

Client ------> SimpleFactory [getProduct(String): Product]--------> Product (interface /abstract)
                                                              --------> Product A
                                                              --------> Product B